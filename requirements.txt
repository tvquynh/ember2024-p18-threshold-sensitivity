# P18 Threshold Sensitivity — runtime dependencies
# Python >= 3.10
# CPU-only (no GPU required)

numpy>=1.24,<2.0
scipy>=1.10
scikit-learn>=1.3
polars>=0.20
lightgbm>=4.6
xgboost>=2.1
matplotlib>=3.7
pytest>=7.0

# PyTorch CPU-only (used by MLP classifier).
# Linux install command (CPU wheel, ~200MB instead of ~2GB CUDA wheel):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
